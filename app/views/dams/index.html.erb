<h1>Dam water levels</h1>

<% @dams.each do |dam| %>
<h3><%= link_to dam.name, dam_path(dam.id) %></h3>
<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Height (m)</th>
      <th>Storage (Ml)</th>
      <th>Current levels (%)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= dam.levels.order("date ASC").last.date.strftime('%d-%b-%y') %></td>
      <td><%= dam.levels.order("date ASC").last.height %></td>
      <td><%= dam.levels.order("date ASC").last.storage %></td>
      <td><%= dam.levels.order("date ASC").last.percentage %></td>
    </tr>
  </tbody>
</table>
<% end %>
